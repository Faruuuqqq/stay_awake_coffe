<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="Jelajahi semua kategori kopi di Stay Awake Coffee. Temukan biji kopi, bubuk kopi, merchandise, dan lainnya.">
    <meta name="keywords" content="kategori kopi, daftar kategori, jenis kopi, kategori produk">

    <!-- Favicons -->
    <link href="/assets/img/favicon.png" rel="icon">
    <link href="/assets/img/apple-touch-icon.png" rel="apple-touch-icon">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,800;1,900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Nunito:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    
    <!-- Vendor CSS Files -->
    <link href="/assets/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/assets/vendor/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
    <link href="/assets/vendor/aos/aos.css" rel="stylesheet">
    <link href="/assets/vendor/glightbox/css/glightbox.min.css" rel="stylesheet">
    <link href="/assets/vendor/swiper/swiper-bundle.min.css" rel="stylesheet">
    <link href="/assets/vendor/drift-zoom/drift-basic.min.css" rel="stylesheet">
    <!-- SweetAlert2 CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11.12.2/dist/sweetalert2.min.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">

    <!-- Template Main CSS File -->
    <link href="/assets/css/main.css" rel="stylesheet">
    
    <style>
        /* Styling khusus untuk halaman kategori */
        .category-card-lg {
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s ease-in-out;
            margin-bottom: 20px;
            background-color: #fff;
            position: relative;
            text-align: center;
        }
        .category-card-lg:hover {
            transform: translateY(-5px);
        }
        .category-card-lg:hover .category-title {
            color: var(--accent-color);
        }
        .category-card-lg:hover .category-image img {
            filter: brightness(1.08);
            transform: rotate(4deg) scale(1.06);
        }
        .category-card-lg .category-image {
            width: 100%;
            height: 120px; /* Tinggi tetap untuk konsistensi */
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem;
            overflow: hidden;
        }
        .category-card-lg .category-image img {
            max-height: 100%;
            max-width: 100%;
            object-fit: contain;
            transition: 0.3s;
        }
        .category-card-lg .category-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: var(--heading-color);
            transition: 0.3s;
        }
        .category-card-lg .category-count {
            font-size: 0.85rem;
            color: color-mix(in srgb, var(--default-color), transparent 30%);
            margin-bottom: 0;
        }
        .category-card-lg .stretched-link {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }
        .loading-spinner-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 200px;
            flex-direction: column;
            gap: 15px;
        }
        .loading-spinner {
            width: 3rem;
            height: 3rem;
        }
    </style>
</head>
<body class="category-page">
    <!-- Header -->
    <%- include('./partials/header.ejs', { user: user, cartItemCount: cartItemCount }) %>

    <main id="main">
        <div class="page-title light-background">
            <div class="container d-lg-flex justify-content-between align-items-center">
                <h1 class="mb-2 mb-lg-0">Categories</h1>
                <nav class="breadcrumbs">
                    <ol>
                        <li><a href="/">Home</a></li>
                        <li class="current">Categories</li>
                    </ol>
                </nav>
            </div>
        </div>

        <section id="categories-list" class="categories section">
            <div class="container" data-aos="fade-up">
                <div class="row gy-4" id="categories-container">
                    <!-- Categories will be loaded dynamically here -->
                    <div class="loading-spinner-container col-12">
                        <div class="spinner-border text-primary loading-spinner" role="status">
                            <span class="visually-hidden">Memuat kategori...</span>
                        </div>
                        <p class="text-center text-muted">Memuat kategori...</p>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <%- include('./partials/footer.ejs') %>

    <!-- Vendor JS Files -->
    <script src="/assets/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/vendor/aos/aos.js"></script>
    <script src="/assets/vendor/glightbox/js/glightbox.min.js"></script>
    <script src="/assets/vendor/swiper/swiper-bundle.min.js"></script>
    <script src="/assets/vendor/purecounter/purecounter_vanilla.js"></script>
    <script src="/assets/vendor/php-email-form/validate.js"></script>
    <script src="/assets/vendor/imagesloaded/imagesloaded.pkgd.min.js"></script>
    <script src="/assets/vendor/isotope-layout/isotope.pkgd.min.js"></script>
    <script src="/assets/vendor/drift-zoom/Drift.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <script src="/assets/js/main.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const categoriesContainer = document.getElementById('categories-container');

            // Fungsi untuk mengambil dan merender kategori
            async function fetchAndRenderCategories() {
                categoriesContainer.innerHTML = `
                    <div class="loading-spinner-container col-12">
                        <div class="spinner-border text-primary loading-spinner" role="status">
                            <span class="visually-hidden">Memuat kategori...</span>
                        </div>
                        <p class="text-center text-muted">Memuat kategori...</p>
                    </div>
                `;

                try {
                    const token = localStorage.getItem('jwtToken'); // Ambil token jika kategori bisa diakses admin/user
                    // console.log('Category Page: Token for fetching categories (if needed):', token); // Debug log

                    const requestHeaders = {
                        'Content-Type': 'application/json'
                    };
                    // Tambahkan Authorization header hanya jika token ada dan diperlukan
                    if (token) {
                        requestHeaders['Authorization'] = `Bearer ${token}`;
                    }
                    // console.log('Category Page: Sending Headers:', requestHeaders); // Debug log


                    // Endpoint untuk mengambil semua kategori dari API
                    const response = await fetch('/api/categories', {
                        headers: requestHeaders // Gunakan header yang sudah disiapkan
                    });

                    if (!response.ok) {
                        const errorData = await response.json();
                        console.error('Category Page: API Error Response for Categories:', errorData);
                        throw new Error(errorData.message || 'Gagal memuat kategori.');
                    }
                    const result = await response.json();
                    const categories = result.data.data; // Sesuaikan jika struktur respons berbeda

                    categoriesContainer.innerHTML = ''; // Hapus indikator memuat

                    if (categories && categories.length > 0) {
                        categories.forEach(category => {
                            const categoryCard = `
                                <div class="col-lg-3 col-md-4 col-sm-6" data-aos="fade-up" data-aos-delay="100">
                                    <div class="category-card-lg">
                                        <div class="category-image">
                                            <img src="${category.image || 'https://placehold.co/300x250/EAEAEA/555555?text=No+Category+Image'}"
                                                 alt="${category.name}" class="img-fluid"
                                                 onerror="this.onerror=null;this.src='https://placehold.co/300x250/EAEAEA/555555?text=No+Category+Image';">
                                        </div>
                                        <div class="card-body">
                                            <h5 class="card-title">${category.name}</h5>
                                            <p class="category-count">${category.productCount !== undefined ? category.productCount : 0} Products</p>
                                            <a href="/products?category=${category.category_id}" class="btn btn-sm btn-view-products">Lihat Produk</a>
                                        </div>
                                    </div>
                                </div>
                            `;
                            categoriesContainer.insertAdjacentHTML('beforeend', categoryCard);
                        });
                    } else {
                        categoriesContainer.innerHTML = '<p class="text-center col-12 text-muted py-5">Tidak ada kategori yang tersedia saat ini.</p>';
                    }
                } catch (error) {
                    console.error('Error fetching categories:', error);
                    categoriesContainer.innerHTML = `
                        <p class="text-center col-12 text-danger py-5">Gagal memuat kategori: ${error.message}</p>
                    `;
                    if (typeof window.showAlert === 'function') {
                        window.showAlert(`Gagal memuat kategori: ${error.message}`, 'error');
                    } else {
                        console.error("window.showAlert is not defined. Falling back to alert.");
                        alert(`Gagal memuat kategori: ${error.message}`);
                    }
                }
            }

            // Panggil fungsi untuk mengambil dan merender kategori saat DOM dimuat
            fetchAndRenderCategories();

            // Panggil updateCartItemCount saat DOM fully loaded
            if (typeof window.updateCartItemCount === 'function') {
                window.updateCartItemCount();
            }
        });
    </script>
</body>
</html>
